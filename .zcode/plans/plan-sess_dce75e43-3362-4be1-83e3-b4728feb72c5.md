## Stitch UI/UX Adoption — 6 Remaining Screens

### Reference
- Design system: `C:\Users\nnand\Downloads\Temp\recall\stitch_recall_design_system\recall_design_system\DESIGN.md`
- Stitch HTML per screen in `stitch_recall_design_system/recall_<screen>/code.html`
- Existing code uses `src/lib/surface.ts` (already matches Stitch tokens), `cardSurface()` helper, Tailwind CSS variables

### Architecture
- SPA with store-based view switching (`useRecallStore` actions: `showDashboard`, `showSettings`, `showStats`, `showBrowser`, `showDeckBrowser`)
- Sidebar navigation in `app-shell.tsx` (NOT bottom nav — keep sidebar, Stitch bottom nav is optional)
- Color tokens already aligned: `bg-surface`, `border-outline-variant`, `bg-primary`, `text-on-surface`, etc.
- `daily-goal.tsx`, `mascot.tsx`, `deck-card.tsx`, `stat-tile.tsx` already exist — restyle, don't rewrite logic

---

### 1. Dashboard (Biggest — Priority 1)

**Files:** `src/components/dashboard.tsx` (rewrite), `src/components/deck-card.tsx` (restyle), `src/components/daily-goal.tsx` (restyle), `src/components/recent-activity.tsx` (NEW)

**New layout — 12-column bento grid:**
```
[max-w-1152px mx-auto px-gutter-mobile py-6]
├── Hero: subtitle + greeting h1 + description + single "Start Review" button
├── Bento Grid (grid-cols-12 gap-5)
│   ├── Daily Progress (col-span-12 md:col-span-8) — bg-surface rounded-2xl
│   │   ├── Left: progress ring/bar, percentage, label "Daily Goal"
│   │   ├── Right: mascot with motivational quote
│   │   └── Background lettermark decoration (low opacity)
│   ├── Streak (col-span-12 md:col-span-4) — bg-primary-soft rounded-2xl
│   │   └── Centered: fire icon, large number, "Current Study Streak"
│   ├── Your Decks header (col-span-12) — title + "View All" link
│   ├── Deck Cards (col-span-12) — grid-cols-1 md:grid-cols-2 lg:grid-cols-3
│   │   ├── DeckCard restyled: 2-char abbreviation, due pill badge, progress bar, New/Learning counts
│   │   └── "Create New Deck" dashed card
│   └── Recent Activity (col-span-12) — title + 2-3 activity items
│       └── Icon circle + title + deck name + card count + relative timestamp
```

**What gets removed from dashboard:**
- `TodayBand` (stats distributed to daily progress + deck cards)
- `ReviewInbox`, `ActivityHeatmap`, `FocusTimer`, `ReviewCalendar` (not in Stitch)
- `LevelTile` (XP/level info)
- 6 import buttons from hero (move to Import page or secondary dropdown)

**What gets restyled:**
- `DailyGoal` → large bento card (span 8), keep progress bar + mascot + confetti logic
- `DeckCard` → 2-char abbreviation, due pill, progress bar, New/Learning counts instead of mastery/accuracy
- `StreakWidget` → compact horizontal, `bg-primary-soft`, no flame tier colors

**New component:**
- `RecentActivity` — reads `reviewLogs` from store, shows last 3-5 sessions with icon, deck name, card count, relative time

---

### 2. Onboarding (Priority 2)

**File:** `src/components/onboarding.tsx` (restyle, logic stays)

Already has 5 steps: `welcome → concept → system → templates → goal`. Restyle each to match Stitch:

- **Welcome:** Add header with Skip button, logo, privacy note, remove feature cards — single "Get Started" CTA
- **Concept:** Add header, restyle SVG timeline to match Stitch, floating card with pulse
- **System:** Add header, two-column layout (left: visual metaphor icon, right: rating buttons + explanation cards), add flashcard preview placeholder
- **Templates:** Add header, restyle template cards (Stitch uses 2-column grid with deck preview data)
- **Goal:** Add header, replace 5 preset buttons with slider (5-100, step 5), add large number display, motivational messages, progress bar at 75%

**Key changes:**
- Header on every screen: logo + Skip button (except first/last screens)
- Step dots moved to bottom nav area (Stitch style)
- Primary button styling: `bg-primary text-on-primary rounded-xl hover:shadow-xl active:scale-95`
- `locales/en.json` + `id.json` — add privacy note, slider labels, motivational messages

---

### 3. Stats Page (Priority 3)

**Files:** `src/components/stats.tsx` (NEW — dedicated page), reuse `stat-tile.tsx`, `activity-heatmap.tsx`, `retention-forecast.tsx`, `workload-forecast.tsx`

**Not a separate route — a view in the existing store-based SPA.** Add `showStats` action already exists in store.

**Layout:**
```
[max-w-1152px mx-auto px-gutter-mobile py-6]
├── Header: "Statistics" title + close button
├── Overview cards (3-col): Total Cards, Retention %, Streak
├── Review Activity (7-day bar chart) — replace heatmap with bar chart
├── Retention Breakdown (segmented bar): Easy/Good/Hard/Again
├── Deck Mastery (circular progress per deck)
└── Review Forecast (horizontal scroll cards)
```

**Reuse existing:** `stat-tile.tsx` (restyle to Stitch), `retention-forecast.tsx`, `workload-forecast.tsx`

**New:** `ReviewActivityBarChart` (7-day bars), `RetentionBreakdown` (segmented bar)

---

### 4. Card Browser (Priority 4)

**File:** `src/components/card-browser.tsx` (rewrite)

**From table-based → card-list layout:**
```
[max-w-1152px mx-auto px-gutter-mobile]
├── Header: "Card Browser" + search + deck filter + state filter
├── Filter chips: All / Due / New / Learning / Review / Suspended
├── Card list (grid-cols-1 md:grid-cols-2 gap-4)
│   └── Card row: front text, tags, due date, state badge, checkbox
└── Bulk actions bar (when selected)
```

**Keep:** search, pagination, deck filter, state filter, bulk select, card dialog, sort — all logic stays
**Change:** table → card grid, add filter chips, add mobile drawer filter

---

### 5. Import Hub (Priority 5)

**Files:** `src/components/import-hub.tsx` (NEW), reuse existing import dialogs

**New unified import page:**
```
[max-w-768px mx-auto px-gutter-mobile py-6]
├── Header: "Import Decks" + close button
├── Drop zone: drag-and-drop area, "Drop .apkg or .csv file"
├── Import method cards (2x2 grid):
│   ├── Anki (.apkg) — icon + description
│   ├── CSV — icon + description
│   ├── Markdown — icon + description
│   └── Manual Entry — icon + description
└── CTA banner: "Start Building Your Knowledge Base"
```

**Reuse:** `anki-import-dialog.tsx`, `csv-import-dialog.tsx`, `markdown-import-dialog.tsx`, `bulk-add-dialog.tsx`

**New:** `import-hub.tsx` page with drop zone, method cards, CTA banner

---

### 6. Add Card Polish (Priority 6)

**File:** `src/components/card-dialog.tsx` (small restyle)

**Changes:**
- Add "Quick Add" info banner at top (`bg-primary-soft rounded-2xl`)
- Add colored accent bars to front/back fields (blue left border for front, amber for back)
- Add dashed border card preview section
- Keep all existing functionality (tabs, cloze, image occlusion, voice input, tags)

---

### Execution Order
1. **Dashboard** — home screen, most impact
2. **Onboarding** — first impression
3. **Stats** — new dedicated page
4. **Card Browser** — restructure UI
5. **Import Hub** — unified page
6. **Add Card** — quick polish

### Files Summary
| File | Action |
|------|--------|
| `src/components/dashboard.tsx` | Major rewrite |
| `src/components/deck-card.tsx` | Restyle |
| `src/components/daily-goal.tsx` | Restyle |
| `src/components/recent-activity.tsx` | NEW |
| `src/components/onboarding.tsx` | Restyle (all 5 steps) |
| `src/components/stats.tsx` | NEW |
| `src/components/card-browser.tsx` | Rewrite (table→cards) |
| `src/components/import-hub.tsx` | NEW |
| `src/components/card-dialog.tsx` | Small restyle |
| `src/locales/en.json` | Add new keys |
| `src/locales/id.json` | Add new keys |

### Skipped
- **Bottom navigation bar** — app uses sidebar, Stitch bottom nav is optional for mobile. ponytail: add when mobile-first redesign
- **Dark mode variants** — already implemented via Tailwind `dark:` classes
- **3D card flip animation** — study-mode already has flip, Stitch's CSS `card-perspective` is cosmetic. ponytail: add when motion polish round
- **Stats page routing** — `showStats` already in store, just needs the component